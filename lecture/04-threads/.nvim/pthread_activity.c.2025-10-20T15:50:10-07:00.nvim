#include <errno.h>
#include <pthread.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

void *mythread(void *arg) {
  printf("Inside a thread!\n");
  return NULL;
}

int main() {

  printf("Hello world!\n");
  pthread_create(thread_id, NULL, my_thread, NULL);
  sleep(5);

  return 0;
}
