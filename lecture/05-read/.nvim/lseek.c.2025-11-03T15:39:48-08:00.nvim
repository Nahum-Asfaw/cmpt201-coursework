#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

int main() {
  char buf[12];

  int fd = open("tmp", O_RDWR | O_CREAT, 0);
  write(fd, "hi\n", strlen("hi\n"));


  int c = close(fd);
}
