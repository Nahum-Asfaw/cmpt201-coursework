// Server
#include <stdio.h>
#include <sys/socket.h>

int main() {
  printf("SERVER:\n");

  // socket
  int socket_fd = socket(domain, type, protocol);
  // bind
  // listen
  // accept
  // read
  // close
  return 0;
}
