// pseduocode:
//  .
//  .
//  .
//
// struct {
// unit64_t size;
// struct header*next;
// };
//
//  // get starting address
//  char *location = sbrk(0)
//

#define _DEFAULT_SOURCE
#define _ISOC99_SOURCE

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

#define BUF_SIZE 1024

void print_out(char *format, void *data, size_t data_size) {
  char buf[BUF_SIZE];
  ssize_t len = snprintf(buf, BUF_SIZE, format,
                         data_size == sizeof(uint64_t) ? *(uint64_t *)data
                                                       : *(void **)data);
  write(STDOUT_FILENO, buf, len);
}

struct header {
  uint64_t size;
  struct header *next;
};

void initilize_first(struct header *block) {
  block->next = NULL;
  block->size = 128;
}

void initilize_second(struct header *first, struct header *second) {
  second->next = first;
  second->size = 128;
}

int main() {
  char *start_address = sbrk(0);
  char *second_address = sbrk(256);
  struct header *first_block = (struct header *)start_address;
  struct header *second_block = (struct header *)second_address;

  print_out(" first_block: ", first_block, sizeof(first_block));
  print_out("\nsecond_block: ", second_block, sizeof(second_block));
  print_out("\nfirst_block size: ", first_block->size,
            sizeof(first_block->size));
  print_out("\nsecond_block size: ", second_block->size,
            sizeof(second_block->size));
  print_out("\nfirst_block next: ", first_block->next,
            sizeof(first_block->next));
  print_out("\nsecond_block next: ", second_block->next, sizeof(second_block->next));

  initilize_first(first_block);
  initilize_second(first_block, second_block);
  memset(first_block, 0, 0);
  memset(second_block, 0, 256);
  print_out("j
}
